import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    video: {
        width: '100%',
        aspectRatio: 16/9,
        marginBottom: 10,
    }
});

export default styles;